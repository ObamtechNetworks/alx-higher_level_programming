#!/usr/bin/node
// write an empty class Rectangle that defines a rectangle

class Rectangle {
  // Empty class in JS
}

// make the module or script visible to the outside
module.exports = Rectangle;
