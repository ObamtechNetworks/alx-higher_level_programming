// apply this when the DOM has loaded
document.addEventListener('DOMContentLoaded', () => {
  // update text color of  header to red
  const element = document.querySelector('header');
  element.style.color = '#FF0000';
});
